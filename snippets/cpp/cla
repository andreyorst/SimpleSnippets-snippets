#ifndef ${3|${2!substitute(substitute(expand('%:t'), '.*', '\U&', 'g'), '\v\..*', '', 'g')}_H}
#define ${4!substitute(substitute(expand('%:t'), '.*', '\U&', 'g'), '\v\..*', '', 'g')}_H

class ${1|Name}
{
public:
	Name();
	virtual ~Name();

private:
	${0:/* data */}
};

#endif /* ${5!substitute(substitute(expand('%:t'), '.*', '\U&', 'g'), '\v\..*', '', 'g')}_H */
